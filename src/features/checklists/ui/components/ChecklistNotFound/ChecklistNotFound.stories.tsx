import ChecklistNotFound from "./ChecklistNotFound";

export default {
    title: "features/checklists/ChecklistNotFound",
    component: ChecklistNotFound,
};

export const Default = () => <ChecklistNotFound />;
